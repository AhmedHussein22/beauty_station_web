// import 'package:rakeez/data/local_storage/hive_storage.dart';
// import 'package:rakeez/features/auth/login/data/user_data_model.dart';

// bool userLogin = false;

// class SaveLocalData {
//   static Future<void> saveUseData(UserModel userData) async {
//     // save user data to local storage by using hive with key and value for each data
//     await HiveStorage.set(HiveKeys.apiTokenKey, userData.userData?.token);
//     await HiveStorage.set(HiveKeys.displayName, userData.userData?.displayName);
//     await HiveStorage.set(HiveKeys.email, userData.userData?.email);
//     await HiveStorage.set(HiveKeys.phoneNumber, userData.userData?.phoneNumber);
//     await HiveStorage.set(HiveKeys.profileImage, userData.userData?.picture);
//   }
// }
// //*************** */
